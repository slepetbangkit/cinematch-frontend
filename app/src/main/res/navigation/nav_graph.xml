<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_graph_home">

    <include
        android:id="@+id/nav_graph_home"
        app:graph="@navigation/nav_graph_home" />

    <include
        android:id="@+id/nav_graph_search"
        app:graph="@navigation/nav_graph_search" />

    <include
        android:id="@+id/nav_graph_activity"
        app:graph="@navigation/nav_graph_activity" />

    <include
        android:id="@+id/nav_graph_profile"
        app:graph="@navigation/nav_graph_profile" />
</navigation>